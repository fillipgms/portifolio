import React from "react";
import imagePlaceholder from "@/public/imagePlaceholder.jpg";
import Image from "next/image";

const Projects = () => {
    return (
        <section className="bg-neutral-200 flex *:flex-1 gap-5 md:px-16 px-10 py-5">
            <h2 className="text-xl font-bold">Projetos</h2>
        </section>
    );
};

export default Projects;
